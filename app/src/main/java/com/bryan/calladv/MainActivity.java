package com.bryan.calladv;

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

public class MainActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);


//        ContactUtils contactUtils=new ContactUtils(this);
//        contactUtils.getPhoneContacts();

//        CallLogUtils callLogUtils=new CallLogUtils(this);
//        callLogUtils.getCallLogs();

       // SearchSMS sms=new SearchSMS(this);
      //  sms.getData(null,0);
     //   sms.getThreadsNum( sms.getThreads(0));
      //  String[] ret= PhoneLocationUtils.getPhoneLocation(this, "13689561258");
       // Toast.makeText(this,ret[0],Toast.LENGTH_SHORT).show();
    }
}
